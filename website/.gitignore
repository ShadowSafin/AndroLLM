# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env (never commit real values; only .env.example is tracked)
.env
.env*.local
# Firebase Admin / service-account keys must never live in the public repo
service-account*.json
*serviceAccount*.json
*-key.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts